

<h1>Categories</h1>
<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
  </tr>
  <% @categories.each do |category| %>
    <tr>
      <td><%= category.id %></td>
      <td><%= category.name %></td>
    </tr>
  <% end %>
</table>
<%= link_to 'New Category', new_admin_category_path %>