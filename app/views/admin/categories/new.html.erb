

<h1>New Category</h1>
<%= form_with model: [:admin, @category], local: true do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <%= form.submit "Create Category" %>
<% end %>